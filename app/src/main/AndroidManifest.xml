<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ugp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UGP">
        <activity
            android:name=".CardDetailActivity"
            android:exported="false" />
        <activity
            android:name=".loginFeatures.PhoneLoginActivity"
            android:exported="true" />
        <activity
            android:name=".loginFeatures.RegisterActivity"
            android:exported="true" />
        <activity
            android:name=".loginFeatures.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ProfileActivity" />
        <activity android:name=".MainActivity">

            <!--
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            -->
        </activity>
        <activity
            android:name=".AboutActivity"
            android:exported="false"
            android:parentActivityName=".BoardActivity" />
        <activity
            android:name=".BoardActivity"
            android:exported="false" />
    </application>

</manifest>